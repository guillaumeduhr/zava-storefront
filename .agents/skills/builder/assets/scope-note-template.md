## Scope boundary

The following were observed during implementation but are **not part of this
PR**. Each is noted here so the human reviewer can decide whether to act on it
separately.

| Area | Observation | Suggested action |
|------|-------------|-----------------|
| `<file or module>` | `<what was noticed — one sentence>` | `<open a ticket / fix in a separate PR / no action needed>` |
